import CourseGoal from "./components/CourseGoal.tsx";

export default function App() {
	return (
		<main>
			<CourseGoal title="Lern React + TS">
				Let's start learniung TS implementation in React
			</CourseGoal>
		</main>
	);
}
